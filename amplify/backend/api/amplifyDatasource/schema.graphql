type TaskCounter @model @auth(rules: [{allow: public}]) {
  id: ID!
  count: Int
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  isComplete: Boolean
}
